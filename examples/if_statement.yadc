void main() {
    int a = 5;
    int b;

    void inner() {
        int c = 10;

        if (c > a) {
            if (a == 5) {
                b = 10;
            }
        }

        if (b == 0) {
            b = 5;
        }
        return;
    }

    inner();
    return;
}