int func1();

int func2();

int main() {

    int b;
    if (true && true && true || true && true && true) {     /* true */
        int a = 1 + 0 + 1 + 0;          /* 1 + 1 */
        b = 2 * 1;                      /* 2 */
        goto label;
    }

    label:
    b = b * 1 / 1 * 2 / 1 + 0;          /* b * 2 */

    return 0;
}

int func1() {
    return 0;
}

int func2() {
    return 0;
}
