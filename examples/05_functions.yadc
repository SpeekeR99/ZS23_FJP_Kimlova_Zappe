/* This is a function prototype */
int sum(int a, int b);

/* This is a function directly with definition */
int sub(int a, int b) {
    return a - b;
}

int main() {
    /* This is a function call */
    int c = sum(1, 2);
    return 0;
}

/* This is a function definition */
int sum(int a, int b) {
    return a + b;
}