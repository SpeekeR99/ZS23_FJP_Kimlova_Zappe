int func1(int a) {
    @(a) = @a + 1;
    return a;
}

int main() {
    int ptr = new(int, 5);

    @(ptr) = 10;
    @(ptr + 2) = 20;
    int a = @(ptr + 2);

    int b = func1(ptr + 1);


    const int size = 5;
    int array = new(int, size);

    int i = 0;
    while (i < size) {
        @(array + i) = i;
        i = i + 1;
    }

    delete(ptr);
    delete(array);

    return 0;
}